version: '3.8'

services:
  kaspa-mcp:
    build: .
    image: kaspa-mcp-server:latest
    stdin_open: true
    tty: true
    environment:
      - KASPA_RPC_URL=http://host.docker.internal:16110
      - DEBUG=true
    networks:
      - kaspa-net

networks:
  kaspa-net:
    driver: bridge